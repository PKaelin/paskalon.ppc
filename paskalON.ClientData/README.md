# Client Data
Loads client data or sample data into the microservices databases.

At this point the data is created in code but later on we might want text files so that testers can run automated tests on their test data.